# 登录密码
ACP_DEMO_PASSWORD=admin

# Cookie 签名密钥（不设置则每次重启后需重新登录）
# ACP_DEMO_SECRET=some-random-secret-string

# 监听地址
# ACP_DEMO_ADDR=127.0.0.1:3000

# kiro-cli 路径（默认从 PATH 查找，也可在 agents.json 中单独配置）
# KIRO_CLI=/path/to/kiro-cli
